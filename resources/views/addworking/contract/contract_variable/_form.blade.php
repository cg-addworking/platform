<fieldset class="mt-5 pt-2">
    <legend class="text-primary h5">@icon('info') {{ __('addworking.contract.contract_variable._form.general_information') }}</legend>

    {{--
    @form_group([
        'text'  => "",
        'name'  => "contract_variable.foo",
        'value' => $contract_variable->foo,
    ])
    --}}
</fieldset>
